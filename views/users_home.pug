extends layout

block title
  if targetUser
    title #{targetUser.userId} - Dgidgi.tal
  else
    title ??? - Dgidgi.tal
    
block content
  main(role='main')
    if targetUser
      // Main jumbotron for a primary marketing message or call to action
      .jumbotron
        .papa-avatar-container
          img.papa-avatar(src='/users/' + targetUser.userId + '/avatar')
      .container
        .row
          .col-4
            div 
              // ici bientôt peut-être quelque chose
          .col-8.dgidgitalisation-liste.p-3
            if posts
              for  post, i in posts
                include dgidgitalisation.pug
