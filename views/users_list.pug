extends layout

block title
  title  Les Dgidgitaliseurs - Dgidgi.tal

block content
  #login-debut-content 
    h1 Les Dgidgitaliseurs
    .album.py-5
      .container#dgidgitaliseur-liste
        .row
          for user,i in allUsers
            .col-md-4
              //
                .card.mb-4.box-shadow
                  img.card-img-top.maman-avatar(src='/users/' + user.userId + "/avatar", alt=user.userName)
                  .card-body
                    br
                    br
                    br
                    br
                    .d-flex.justify-content-between.align-items-center
                      .btn-group
                        button.btn.btn-sm.btn-outline-secondary(type='button', href='/toto') View
                  small.text-muted  #{user.userName}
            .card.mb-4.box-shadow
              a(href='/users/' + user.userId)
                img.card-img-top.maman-avatar(src='/users/' + user.userId + "/avatar", alt=user.userName)
              .card-body
                p.card-text
                  | blahblahblahblahblahblahblahblahblahblahblahblahblahbl
                .d-flex.justify-content-between.align-items-center
                  br
                  small.text-muted  #{user.userName}
                    a(href='/users/' + user.userId)
